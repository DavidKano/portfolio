---
import Base from "../layouts/BaseLayout.astro";
import NavBar from "../components/NavBar.astro";
import SiteFooter from "../components/SiteFooter.astro";

const EMAIL = "davidkano@hotmail.es";
const LINKEDIN = "https://www.linkedin.com/in/david-cano-jaramillo-6bb1b017b/";
const GITHUB = "https://github.com/DavidKano"; // opcional, puedes quitarlo
---
<Base title="David Cano — Contacto">
  <NavBar />

  <main class="bg-white">
    <!-- Hero -->
    <section class="mx-auto max-w-6xl px-6 py-12 md:py-16 text-center">
      <h1 class="text-3xl md:text-4xl font-extrabold tracking-tight">📬 ¿Hablamos?</h1>
      <p class="mt-3 text-zinc-600 max-w-2xl mx-auto">
        Si te encaja lo que has visto y crees que podemos colaborar, estaré encantado de escucharte.
        Escríbeme por email o conéctame por LinkedIn.
      </p>

      <div class="mt-8 flex flex-wrap items-center justify-center gap-3">
        <a href={`mailto:${EMAIL}`}
           class="inline-flex items-center gap-2 rounded-lg bg-zinc-900 text-white px-4 py-2 hover:bg-zinc-800 transition">
          <span>📧</span> Enviarme un email
        </a>
        <a href={LINKEDIN} target="_blank" rel="noopener"
           class="inline-flex items-center gap-2 rounded-lg ring-1 ring-zinc-300 px-4 py-2 hover:bg-zinc-50 transition">
          <span>💼</span> Conectemos en LinkedIn
        </a>
        <a href={GITHUB} target="_blank" rel="noopener"
           class="inline-flex items-center gap-2 rounded-lg ring-1 ring-zinc-300 px-4 py-2 hover:bg-zinc-50 transition">
          <span></span> GitHub
        </a>
      </div>

      <p class="mt-3 text-sm text-zinc-500">Abierto a nuevas oportunidades 🚀</p>

      <!-- Tarjeta con datos rápidos -->
      <div class="mt-10 mx-auto max-w-xl rounded-2xl ring-1 ring-zinc-200 bg-white p-6 text-left">
        <dl class="grid grid-cols-1 sm:grid-cols-2 gap-4">
          <div>
            <dt class="text-xs uppercase tracking-wide text-zinc-500">Email</dt>
            <dd class="mt-1">
              <a id="email-link" href={`mailto:${EMAIL}`} class="text-zinc-900 hover:underline">{EMAIL}</a>
            </dd>
          </div>
          <div>
            <dt class="text-xs uppercase tracking-wide text-zinc-500">LinkedIn</dt>
            <dd class="mt-1">
              <a href={LINKEDIN} target="_blank" rel="me noopener" class="text-zinc-900 hover:underline">
                /in/david-cano-jaramillo-6bb1b017b
              </a>
            </dd>
          </div>
        </dl>
        <button id="copy-email"
                class="mt-6 text-sm rounded-md ring-1 ring-zinc-300 px-3 py-1.5 hover:bg-zinc-50">
          Copiar email
        </button>
      </div>
    </section>
  </main>

  <SiteFooter />

  <script>
    // Copiar al portapapeles
    const btn = document.getElementById('copy-email');
    const email = document.getElementById('email-link')?.textContent || "";
    btn?.addEventListener('click', async () => {
      try { await navigator.clipboard.writeText(email?.trim()); btn.textContent = '¡Copiado! ✅'; }
      catch { btn.textContent = 'No se pudo copiar 😅'; }
      setTimeout(() => (btn.textContent = 'Copiar email'), 1800);
    });
  </script>
</Base>
